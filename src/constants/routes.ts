const AppRoutes = {
  APP_DEFAULT: '/',
  APP_HOMEPAGE: '/',
  APP_LOGIN: '/login',
  APP_REGISTER: '/register',
  APP_PROFILE: '/profile',
  APP_PRODUCT_DETAIL: ':nameId'
} as const

export default AppRoutes
